import React from "react";
import { shallow } from "enzyme";
import StackedNav from "./StackedNav";

describe("[ClientApp] ", () => {
	describe("StackedNav ", () => {
		it("Mounts as a react component", () => {
			// const wrapper = shallow(<StackedNav />);
			// const el = wrapper.find("nav");
			// expect(el).toExist();
		});
	});
});
